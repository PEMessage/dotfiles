#!/bin/sh

for x in 1 2 3 4 5 6 7 8 9
do
LIST="
$LIST
$(man -k -s $x . 2>/dev/null)
"
done

pagename="$(echo "$LIST" | sed '/^[[:blank:]]*$/d'  | fzf | cut -d ' ' -f 1 )"

if which vman >/dev/null 2>&1 ; then
    vman $pagename
else
    man $pagename
fi

